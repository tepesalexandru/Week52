import React, { ReactElement } from 'react'

interface Props {
    
}

export default function CreateWeekPlan({}: Props): ReactElement {
    return (
        <div>
            Week plan: Add Goals for the week, their tasks and duration in minutes
        </div>
    )
}
