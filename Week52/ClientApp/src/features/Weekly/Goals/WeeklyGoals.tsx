import React, { ReactElement } from 'react'

interface Props {
    
}

export default function WeeklyGoals({}: Props): ReactElement {
    return (
        <div>
            Weekly Goals
        </div>
    )
}
